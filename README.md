# dev_setup

Bootstrap a new Mac with my preferred settings.

## What it does

- Installs [iTerm2](https://iterm2.com)
- Symlinks `.zshrc` to `~/`

## Usage

```sh
git clone https://github.com/pjazayeri/dev_setup.git
cd dev_setup
bash setup.sh
```

Then restart your terminal or run `exec zsh`.
